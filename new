Процедура СоздатьЗаказДоставки(Клиент, Адрес, СписокБлюд)

    // Создаем новый документ "ЗаказДоставки"
    Заказ = Документы.ЗаказДоставки.СоздатьДокумент();

    // Заполняем данные клиента и адрес доставки
    Заказ.Клиент  Клиент;
    Заказ.АдресДоставки = Адрес;

    // Добавляем блюда в состав заказа
    Для Каждого Блюдо Из СписокБлюд Цикл
        НоваяСтрока = Заказ.Товары.Добавить();
        НоваяСтрока.Номенклатура = Блюдо.Наименование;
        НоваяСтрока.Количество = Блюдо.Количество;
    КонецЦикла;

    // Записываем заказ в базу и проводим
    Заказ.Записать();
    Заказ.Провести();

    Сообщить("Заказ на доставку успешно создан.");

КонецПроцедуры